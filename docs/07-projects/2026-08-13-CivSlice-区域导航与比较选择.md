---
title: CivSlice 区域导航与比较选择
category: projects
tags: [CivSlice, 区域, 时间轴, 泳道, 比较, UI]
status: published
created: 2026-08-13
updated: 2026-08-13

> 实现仓库：[CivSlice](https://github.com/jk9988610/CivSlice)  
> **本文件为时间轴交互的 v2 主设计**，取代「全球统一时代段优先」方案。  
> 关联：[时间轴交互流程](2026-08-12-CivSlice-时间轴交互流程.md) · [对比雷达与派生指标](2026-08-12-CivSlice-对比雷达与派生指标.md)

## 核心流程

```
① 区域 → ② 时间段 → ③ 多选国家（泳道） → ④ 段内时间轴 → ⑤ 比较维度勾选
```

| 步骤 | 用户操作 | 系统行为 |
|------|----------|----------|
| ① 区域 | 选大洲或「世界」 | 加载该区域的国家池与专属时间段列表 |
| ② 时间段 | 选该区域的一个时段 | 收窄时间轴；切换维度集；过滤可用国家 |
| ③ 国家 | 多选加入 / 取消移除 | 选中 → 出现泳道；取消 → 删除泳道 |
| ④ 时间轴 | 拖滑块、点泳道圆点 | 更新当前聚焦快照与剖面雷达 |
| ⑤ 比较 | 勾选参与对比的国家 | 对比雷达仅含勾选国；默认全选 |

---

## ① 区域选择

采用**自然地理大洲**划分，不再使用西欧/东欧/中东等亚区分。

### 区域列表（7 大洲 + 世界）

| id | 显示名 | 说明 |
|----|--------|------|
| `europe` | 欧洲 | 含地中海欧洲、北欧、东欧等 |
| `asia` | 亚洲 | 含东亚、南亚、西亚、中亚、东南亚 |
| `africa` | 非洲 | 含北非、撒哈拉以南非洲 |
| `oceania` | 大洋洲 | 含澳大利亚、新西兰、太平洋岛屿 |
| `north_america` | 北美洲 | 含中美洲以北 |
| `south_america` | 南美洲 | |
| `antarctica` | 南极洲 | 通常无古代文明记录，多为空状态 |
| `world` | 世界 | 跨洲总览，用于全球同时代比较 |

**推荐 Tab 顺序**：`世界` 置首或置尾突出；其余按洲名字母或固定地理顺序排列。

### 行为

- 切换区域 → **重置** ②③④⑤ 全部选择
- **大洲模式**：国家池 = `regions` 含当前洲且有当前段快照的文明
- **世界模式**：国家池 = 该时段内**全球所有**有快照的文明（忽略洲过滤）
- 一国可属多洲（如土耳其：`["asia", "europe"]`）；在非「世界」模式下，出现在其所辖各洲的国家列表中

```json
{
  "id": "china",
  "name": "中国",
  "regions": ["asia"],
  "color": "#b22222"
}
```

```json
{
  "id": "egypt",
  "name": "古埃及",
  "regions": ["africa"],
  "color": "#c9a227"
}
```

```json
{
  "id": "turkey",
  "name": "土耳其",
  "regions": ["asia", "europe"]
}
```

### 南极洲

- 默认无历史文明快照；UI 显示「南极洲暂无文明记录」
- 若未来添加科考站/条约时代等当代条目，仍归 `antarctica`

### 世界（统称）

- 用于**跨洲共时比较**（如「公元前 500 年：中国 vs 埃及 vs 希腊」）
- 使用 `meta.regions` 中 `id: "world"` 的专属 `periods[]`（全球视角时段，如「轴心时代」「大航海时代」）
- 选「世界」时，国家列表展示全球所有在该段有数据的文明

---

## ② 时间段（按区域配置）

> **各区域可有不同的时间段划分与标签，不必全球统一模板。**

### 原则

- 时间段列表定义在 `meta.regions[].periods[]`，非全局单一列表
- 每段仍绑定 `eraTemplate`（决定剖面维度集），但 `yearMin`/`yearMax`/`label` 可按区域定制
- 例：亚洲可有「春秋战国」「唐宋」；非洲可有「古埃及王国」「萨赫勒帝国」；世界可有「轴心时代」「大航海时代」

### 数据

```json
{
  "meta": {
    "regions": [
      {
        "id": "world",
        "label": "世界",
        "periods": [
          {
            "id": "world_axial",
            "label": "轴心时代",
            "yearMin": -800,
            "yearMax": -200,
            "eraTemplate": "iron_imperial"
          },
          {
            "id": "world_early_modern",
            "label": "大航海时代",
            "yearMin": 1400,
            "yearMax": 1800,
            "eraTemplate": "early_modern"
          }
        ]
      },
      {
        "id": "asia",
        "label": "亚洲",
        "periods": [
          {
            "id": "asia_bronze",
            "label": "青铜时代",
            "yearMin": -4000,
            "yearMax": -1000,
            "eraTemplate": "bronze"
          },
          {
            "id": "asia_imperial",
            "label": "帝制时代",
            "yearMin": -221,
            "yearMax": 1912,
            "eraTemplate": "iron_imperial"
          }
        ]
      },
      {
        "id": "africa",
        "label": "非洲",
        "periods": [
          {
            "id": "africa_ancient",
            "label": "古代北非王国",
            "yearMin": -3500,
            "yearMax": -500,
            "eraTemplate": "bronze"
          }
        ]
      },
      {
        "id": "europe",
        "label": "欧洲",
        "periods": [
          {
            "id": "europe_classical",
            "label": "古典时代",
            "yearMin": -800,
            "yearMax": 500,
            "eraTemplate": "iron_imperial"
          }
        ]
      }
    ]
  }
}
```

### 行为

- 选时段 → 重置③④⑤
- 主时间轴 `min`/`max` = 该段 `yearMin`/`yearMax`
- 剖面雷达维度 = 该段 `eraTemplate`
- 顶栏：`亚洲 · 帝制时代 · 公元前 221 — 1912` 或 `世界 · 轴心时代 · 公元前 800 — 前 200`

---

## ③ 国家多选与泳道

### 国家选择器

- 展示当前**区域 + 时段**内有快照的国家列表
- **多选 Checkbox**：选中加入泳道，取消则从泳道移除
- 无单选限制；可选 0~N 国
- 选 0 国时：泳道区空，主时间轴与雷达显示占位提示

### 泳道规则

```
选中 中国、日本、朝鲜 → 泳道显示 3 行
取消 日本            → 日本泳道消失，日本从比较池移除
```

| 操作 | 泳道 | 比较默认 |
|------|------|----------|
| 勾选国家 | 新增一行 | 该国**自动加入**比较（默认参与） |
| 取消勾选 | 删除该行 | 该国从比较池**移除** |
| 段内圆点 | 不变 | 点击圆点 → ④ 主时间轴聚焦该国该年 |

```javascript
function civilizationsInScope(regionId, period, civilizations) {
  const inPeriod = (civ) =>
    civ.snapshots.some(
      s => s.year >= period.yearMin && s.year <= period.yearMax
    );

  if (regionId === 'world') {
    return civilizations.filter(inPeriod);
  }
  return civilizations.filter(
    c => c.regions?.includes(regionId) && inPeriod(c)
  );
}

function swimlanes(regionId, period, civilizations, selectedCountryIds) {
  return selectedCountryIds
    .map(id => civilizations.find(c => c.id === id))
    .filter(Boolean)
    .map(civ => ({
      id: civ.id,
      name: civ.name,
      color: civ.color,
      markers: civ.snapshots.filter(s =>
        s.year >= period.yearMin && s.year <= period.yearMax
      ),
    }))
    .filter(lane => lane.markers.length > 0);
}
```

---

## ④ 段内主时间轴

- 范围 = 当前时段 `yearMin` ~ `yearMax`
- **聚焦国**：泳道点击或比较面板选中时的「当前查看」国家（剖面雷达主对象）
- 滑块拖动 → 更新聚焦国在当前段的最近快照
- 与 [泳道总览](2026-08-12-CivSlice-时间轴交互流程.md#②½-各国泳道时间轴阶段内总览) 联动

---

## ⑤ 比较维度勾选

> 泳道中的国家**默认全部参与**维度比较；可单独取消某国，使其不参与对比雷达，但**泳道保留**（若仍勾选在③）。

### 两层选择（重要）

| 层级 | 控制 | 影响 |
|------|------|------|
| **泳道成员**（③） | 国家 Checkbox | 是否显示泳道行 |
| **比较参与**（⑤） | 比较面板 Toggle | 是否进入对比雷达/派生指标 |

默认：加入泳道 → `comparisonActive` 自动包含该国。

### 比较面板 UI

位于对比视图（或剖面旁栏），泳道国家列表：

```
比较参与：
  [✓] 中国    [✓] 日本    [ ] 朝鲜
       ↑ 点击取消 → 朝鲜仍保留泳道，但对比雷达不画朝鲜多边形
```

| 控件 | 行为 |
|------|------|
| 全选 / 全不选 | 批量切换比较参与 |
| 单击某国 Toggle | 仅影响对比雷达，不删泳道 |
| 对比雷达 | 只绘制 `comparisonActive` 中的国家 |
| 时代均值背景 | 基于 `comparisonActive` 计算（n≥2） |

### 状态模型

```javascript
const uiState = {
  regionId: 'asia',
  periodId: 'asia_imperial',
  swimlaneMembers: ['china', 'japan', 'korea'],  // ③ 勾选
  comparisonActive: ['china', 'japan'],           // ⑤ 比较参与（⊆ swimlaneMembers）
  focusCountryId: 'china',                        // ④ 剖面主视图
  focusYear: 1000,
};

// ③ 新加入国家时
function onCountryAddToSwimlane(countryId) {
  swimlaneMembers.push(countryId);
  if (!comparisonActive.includes(countryId)) {
    comparisonActive.push(countryId);  // 默认参与比较
  }
}

// ③ 从泳道移除时
function onCountryRemoveFromSwimlane(countryId) {
  swimlaneMembers = swimlaneMembers.filter(id => id !== countryId);
  comparisonActive = comparisonActive.filter(id => id !== countryId);
}

// ⑤ 仅切换比较参与
function onToggleComparison(countryId) {
  if (comparisonActive.includes(countryId)) {
    comparisonActive = comparisonActive.filter(id => id !== countryId);
  } else if (swimlaneMembers.includes(countryId)) {
    comparisonActive.push(countryId);
  }
}
```

### 对比雷达规则

- 最多同时绘制 **5** 国（可读性上限，超出时提示）
- `comparisonActive` 为空 → 对比雷达显示「请至少选择一国参与比较」
- 剖面雷达（十维）始终显示 **focusCountryId** 的单国快照，不受⑤影响

---

## 完整 UI 线框

```
┌────────────────────────────────────────────────────────────┐
│ 区域  (世界)(欧洲)(亚洲)(非洲)(大洋洲)(北美洲)(南美洲)(南极洲)   │
├────────────────────────────────────────────────────────────┤
│ 时段  [青铜时代] [帝制时代] [近代] …  ← 随区域变化           │
├────────────────────────────────────────────────────────────┤
│ 国家  ☑中国 ☑日本 ☐朝鲜 ☐越南 …  ← 多选，控制泳道           │
├────────────────────────────────────────────────────────────┤
│ 泳道   中国  ────○───○───────○                              │
│        日本  ──○─────○                                      │
├────────────────────────────────────────────────────────────┤
│ 主时间轴  ◀ ────────●────────────────────────────── ▶      │
├────────────────────────────────────────────────────────────┤
│ [剖面] [对比]                                               │
│ 比较参与: [✓中国] [✓日本] [ ]朝鲜   ☐时代均值               │
├──────────────┬─────────────────────────────────────────────┤
│ 雷达         │ 维度 / 指标卡片                              │
└──────────────┴─────────────────────────────────────────────┘
```

---

## 视图分工

| 视图 | 数据范围 | 受⑤影响 |
|------|----------|---------|
| 剖面雷达 | `focusCountryId` 单国 | 否 |
| 对比雷达 | `comparisonActive` 多国 | 是 |
| 泳道 | `swimlaneMembers` | 否（③控制） |
| 维度卡片（剖面） | 聚焦国 | 否 |
| 维度卡片（对比） | 比较参与国并排 | 是 |

---

## 与旧设计的关系

| 旧（v1） | 新（v2，本文） |
|----------|----------------|
| 全球统一 7 时代段 | **区域** 各自 `periods[]` |
| 先时段后国家 | **先区域 → 时段 → 国家** |
| 对比模式 Checkbox | **⑤ 比较参与 Toggle**，默认全选 |
| 泳道自动显示段内所有国 | 泳道仅显示**用户勾选**的国 |

[时间轴交互流程](2026-08-12-CivSlice-时间轴交互流程.md) 中泳道计算、联动逻辑仍适用，嵌入本流程③④步。

---

## 实现优先级

| 阶段 | 内容 |
|------|------|
| P1 | `meta.regions` 八大区域（7 洲 + 世界）+ 每区域 `periods` |
| P2 | ①② 区域/时段 Tab，切换重置下游 |
| P3 | ③ 国家多选 ↔ 泳道增删 |
| P4 | ④ 主时间轴 + 泳道圆点联动 |
| P5 | ⑤ 比较参与 Toggle + 对比雷达过滤 |
| P6 | 文明 `regions[]` 字段、跨国多区域 |
| P7 | localStorage 记忆选择 |

---

## 禁止事项

- 使用西欧/东欧/中东等亚区 id（已废弃，统一用大洲 id）
- 未选区域即加载国家/泳道
- 从泳道移除却仍在 `comparisonActive` 中
- 对比雷达包含未在 `comparisonActive` 中的国家
- 剖面雷达同时叠超过 1 国（剖面始终单国聚焦）

---

## 相关

- [时间轴交互流程](2026-08-12-CivSlice-时间轴交互流程.md)
- [时代维度模板](2026-08-12-CivSlice-时代维度模板.md)
- [对比雷达与派生指标](2026-08-12-CivSlice-对比雷达与派生指标.md)
- [AI 工作指南](../AI-GUIDE.md)
